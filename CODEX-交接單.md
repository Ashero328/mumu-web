# Codex 交接單（mumu-web）

最後更新：**2026-09-05**

接手先讀這份，再讀 `README.md`。
分工是：**這份講「現在是什麼狀態、哪裡有雷、下一步做什麼」，`README.md` 講「怎麼做、為什麼這樣做」。**
細節不在這裡重複，只給指路。

---

## 1. 這個 repo 是什麼

設計印刷工作室「木木文化」的官網。**純靜態，沒有建置流程、沒有框架、沒有套件管理。**
把資料夾丟進任何一台 http server 就是完整網站，GitHub Pages 直接吃。

唯一的編譯只有 SCSS → CSS，其餘（HTML、JS）都是手寫、直接上線的成品。

```bash
# 本機預覽（一定要用 http，不能用 file://）
python -m http.server 8000
# → http://127.0.0.1:8000/
```

> `file://` 會壞掉的東西：`e-cards.html` 的 `?f=` 查詢參數、剪貼簿 API、
> 賀卡的 `?min=1` 純淨模式。要驗證一律起 server。

---

## 2. 三條不能破的鐵律

| 鐵律 | 為什麼 |
|---|---|
| **只改 `.scss`，`.css` 是產物** | 改了 `.css` 下次編譯就被蓋掉。編譯：`npx sass --update css:css cards:cards clients:clients --no-source-map` |
| **導覽／第二層導覽／頁尾只改 `_partials/`** | 頁面裡的是產物。改完跑 `build_pages.py` 同步三頁，提交前跑 `build_pages.py --check` |
| **動態一律 GSAP，且必附 `prefers-reduced-motion` 保護** | 全站一致；使用者關掉動態時要真的關掉 |

補充規則：

- **輪播一律用 Owl Carousel**，不要自己寫（套件在 `scripts/owl-carousel/`）
- **燈箱一律用 Fancybox**（`scripts/fancybox/`），不要自製 `<dialog>`
- **不寫 inline style／inline script**，CSS 歸 `.css`、JS 歸 `.js`
- **每個獨立網站一包、自給自足**：`cards/` 與 `clients/` 底下每一包都自帶 GSAP 等副本，
  不引用主站的檔案。**這是刻意的**，不要「順手去重」——已交付的卡不能被之後的改動影響

---

## 3. 現在有哪些頁

| 頁面 | 狀態 |
|---|---|
| `index.html` | 首頁。**已依設計稿校版完成**，動它之前看第 6 節 |
| `e-cards.html` | 電子賀卡公版目錄（93 款，可篩選／載入更多／挑款清單） |
| `custom-cards.html` | 客製案例。主展示 ＋ 下方縮圖列切換 |
| `card-preview.html?code=款號` | 單款預覽頁。公版沒有對應動態版時，點卡片會來這裡 |
| `cards/<卡號>/` | 木木自己的範本卡，一張卡＝一個獨立小網站 |
| `clients/<代號>/` | 客戶的成品（賀卡或網站都算） |

**還沒做的內頁**：名片、紙張與價格、與我聯絡（含訂單流程）、`works.html`（作品實績）。
沒做的原因多半是**素材還沒到**，不是技術問題。

---

## 4. 2026-09-05 這一輪做了什麼

### 修好：客製案例的縮圖列點不動

`scripts/custom-showcase.js` 的拖曳捲動在 `pointerdown` 就呼叫 `setPointerCapture()`。

> **pointer capture 會把之後的 `mousedown` / `mouseup` / `click` 一起改派到捕捉的元素上。**
> 於是 click 的 target 變成 `.case-showcase__thumbs` 容器，
> 掛在每顆 `.case-thumb` 上的 click 監聽器永遠收不到 —— 六顆縮圖全部沒反應。

難認的地方：**console 完全乾淨、CSS 沒問題、按鈕也沒有被蓋住**，畫面上看不出哪裡壞。
箭頭與鍵盤方向鍵照常運作，因為它們不經過 pointer capture。

改法是**只有真的拖超過 5px 門檻才捕捉指標**，單純點選完全不碰 pointer capture；
放手時把「拖曳過」記成 `suppressClick` 吃掉瀏覽器補的那一發 click，
並在下次 `pointerdown` 清掉（原本拿 `dragging` 兼差，`pointercancel` 之後會殘留、誤吃下一次點擊）。

> **通則**：拖曳捲動要嘛用門檻延後捕捉，要嘛就改用事件委派把 click 收在容器上。
> 「一按下去就 setPointerCapture」跟「子元素要能被點」不能並存。

---

## 5. ⚠️ 文件與程式不一致的地方（接手前一定要知道）

**`README.md` 的「客製案例：作品集總覽（2026-09-03 定版）」那一節已經過時。**
它寫的是 `.portfolio-grid` 一次列出所有作品、用 Fancybox Modal 播放；
但現在 `custom-cards.html` 實際上是 **`.case-showcase`：左右分割的主展示 ＋ 下方縮圖列切換**，
動畫用 inline `<iframe>` 播放，**這一頁沒有載入 Fancybox**。

連帶的殘留：

- `css/e-cards.scss` 裡還有 `.portfolio-card__enter` 等規則，**HTML 已經沒有任何地方在用**（dead CSS）
- 要不要清掉、以及 README 那一節要怎麼改寫，**請先問用戶**，不要自己決定客製案例的呈現方式——
  這一頁的形式被改過很多輪，每一輪都是用戶指定的

> 判斷原則：**程式是真相，文件可能落後。** 兩者衝突時以程式為準，然後回報給用戶。

---

## 6. 🔴 兩個待確認的發現（2026-09-05 盤點出來的，還沒動手）

### (1) 105 張聖誕公版圖躺在 repo 裡，但網頁上完全看不到

`images/e-cards/thumb/` 與 `full/` 各有 198 張圖，但 `scripts/e-cards-data.js` 只登記了 93 張：

| 款號前綴 | 圖檔張數 | 資料檔登記 |
|---|---|---|
| `ENY` / `ENYH`（西元新年） | 16 / 4 | ✅ 16 / 4 |
| `EY8` / `EY8H`（羊年） | 36 / 16 | ✅ 36 / 16 |
| `EY0` / `EY0H`（農曆年） | 13 / 8 | ✅ 13 / 8 |
| **`EYC` / `EYCH`（看編號應為聖誕）** | **61 / 44** | ❌ **完全沒登記** |

轉檔已經做完了，只差在 `公版賀卡` 陣列補一組。但**節日顯示名稱、導引文案、要排在第幾順位**
都是內容決定，**要問用戶**，不要自己填。

順帶：這也讓 `CLAUDE.md` 裡「公版沒有聖誕」那句話失效了。

### (2) `EYCH040` 缺號

`EYCH` 編號到 045，但只有 44 個檔，**`EYCH040` 在 thumb 與 full 都不存在**。
資料檔是用「數量 + 流水號」推出檔名的，所以要是直接填 `直式: 45`，那一張會 404。
補圖或改成跳號都要先確認。

---

## 7. 動 `index.html` 之前必讀

首頁是**照設計師的 PS 版型逐項校版過的**（版面基準：`--wrap` 1920、內容欄 1440、輪播 1640）。
過去每一輪動到首頁，都要拿 `git show HEAD:index.html` 另存一份，
兩個版本同時渲染再**逐項比對 DOM 座標**（導覽、hero、about、頁高…），在多個斷點確認完全相同。

**只是改連結文字或 `href` 也要驗**——中文字寬會改變 flex 的分配。

其他容易白忙的坑（完整清單在 `README.md`）：

- **巢狀的 `@media` 一律放在該 SCSS 規則的最後一行**。後面若還有宣告，
  會被收成第二條同名規則排在媒體查詢之後、把媒體查詢裡的值蓋掉。症狀很像「樣式沒套到」
- **`body { overflow-x: clip }` 擋不住溢出**（會上拋給 viewport），要修在真正溢出的那個區塊上。
  而且 `overflow: clip` 之下 `scrollWidth > clientWidth` 會虛報，
  要用 `window.scrollTo(99999,0)` 再讀 `scrollX` 才準
- **綠色分兩支**：`--green` 只給填色與線條，**所有綠色的「字」一律 `--green-deep`**
  （`--green` 當文字色只有 4.00:1，沒到 4.5 門檻）
- **任何 `focus()` 都要想它會不會捲動畫面**，需要時加 `preventScroll: true`
- **Lenis 啟用時 `html { scroll-behavior: smooth }` 一定要關掉**，兩層補間會拖泥帶水；
  錨點用 `lenis.scrollTo(el)`，**不要自己算 offset**（它已經含 `scroll-margin-top`）

---

## 8. 怎麼驗證

⚠️ **校版與驗證腳本不在這個 repo 裡**（放在專案資料夾的 `_驗證工具\`，刻意不進 git）。
沒有那包的話，最低限度自己用 Playwright 起 http server 跑這幾項：

```
七個頁面 × 桌機／手機兩檔，每一頁確認：
  console 錯誤 0 ／ 404 0 ／ 橫向捲軸 0
互動：篩選、載入更多、挑款清單、客製案例縮圖切換、鍵盤方向鍵與 Enter
對比：本文 4.5:1、大字與非文字元件 3:1
```

**截圖驗證的兩個陷阱：**

1. **不要用「等 N 毫秒」賭 GSAP 跑完**，觸發時間會隨圖片載入快慢浮動。
   做法是把每一條**有限長度**的 tween `progress(1)` 快轉到結束（無限循環的要跳過）。
   症狀：整片區塊在截圖上一片空白，其實元素都在、只是停在 opacity 0
2. **量「版面是否對齊」要用 `offsetWidth` / `offsetTop`，不要用 `getBoundingClientRect`**。
   3D 輪播會對每一格下 scale／rotateY，量到的視覺尺寸本來就不一樣，那是深度不是 bug

2026-09-05 推送前跑過的：七頁 × 兩檔全部 console 0／404 0／橫向捲軸 0；
三支 SCSS 重新編譯與 repo 內的 `.css` **零差異**；`build_pages.py --check` 三頁皆為最新。

---

## 9. 部署 SOP

```bash
# 1. 改過 .scss？
npx sass --update css:css cards:cards clients:clients --no-source-map

# 2. 改過 _partials/？
python <專案資料夾>/_驗證工具/build_pages.py
python <專案資料夾>/_驗證工具/build_pages.py --check   # ← 提交前一定要跑

# 3. 推
git add -A && git commit && git push        # GitHub Pages 自動更新
```

> **`build_pages.py --check` 忘了跑的話**，partial 改了但頁面沒更新，
> 這種不一致從畫面上完全看不出來。

**搬動或複製賀卡時，`og:url` 與 `og:image` 一定要跟著改成新路徑，而且要用絕對網址。**
LINE／FB 的爬蟲只讀原始碼、不執行網頁，相對路徑它找不到基準，預覽會抓到別張卡或 404。

---

## 10. 跟用戶合作的方式

- **有疑問就馬上問。** 不確定、或有多種做法時直接提出來討論，不要自己選一個做下去
- **內容決定（文案、分類、要放哪些案例）一律問**，不要自己填
- **做完先回報**，不要主動問「要不要幫你開起來看」
- **改完要說清楚動到了什麼、驗了什麼**，測試沒過就照實講

### 兩個必須守住的誠實標記

1. **`clients/songchuan`（松川珈琲焙煎所）是虛構品牌的示意卡**，不是真的客戶案例。
   `custom-cards.html` 裡它的名稱旁邊留著「品牌示意」——**拿掉就等於在官網上宣稱做過這個案子**
2. **客製案例的第 04～06 件是「版面示意」佔位**，同理，標記不能拿掉。
   真案例進來就換掉它們

---

## 11. 待辦

**內容等素材**

- [ ] 105 張聖誕公版上架（見第 6 節，要先確認節日名稱與排序）
- [ ] `EYCH040` 缺號要補圖還是跳號
- [ ] 首頁 `#works` 那 8 張名片圖是 4 張重複湊的佔位，來源未確認
- [ ] 首頁賀卡第二頁仍是第一頁的複製品（桌機會出現兩顆圓點、內容一樣）
- [ ] 其餘內頁：名片／紙張與價格／與我聯絡（含訂單流程）／`works.html`

**技術債**

- [ ] `README.md` 客製案例那一節要改寫（見第 5 節）
- [ ] `.portfolio-card__*` dead CSS 要不要清掉
- [ ] 桌機 `.contact__top p` 對比 4.08～4.28:1，差一點到 4.5。
      三條改法都會動到已校版的桌機版面，**要改請先問用戶**
- [ ] `e-cards.html` 的檔期文案目前寫死（「年末年初是旺季，建議提前四週」），到三月會怪

**問用戶**

- [ ] 首頁「作品」大標要不要跟其他大標統一字級
- [ ] 首頁 `#promo` 段落現在只能從主視覺的按鈕進入，定位要不要調整
